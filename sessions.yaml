# sessions.yaml Schema:
# ```yaml
# sessions: <SessionID: SessionEntry>
# ignored: <SessionID[]>   # A list of zero or more session ids to not include in the `sessions` object.
# ```

# SessionEntry Schema:
# ```yaml
#   description (str OR str[]): A 1–2 phrase summary of the purpose of the session (why did it take place) and progress (what was done). Include no implementation details. If the session is exceptionally long, this field may be a list, allowing the session to be split into “chapters,” with each chapter given its own description string.
#   custom_title (optional str): if explicitly provided in the input.
#   directories (str[]): a list of the session’s directory and any root directories of other repos if such repos where actively researched or modified.
#   updated_when_message_count_was (int): the number of messages in the session when it was last updated. This data should be available from the conversation’s metadata.
#   keywords (optional str[]): 1–3 session-specific, domain-specific keywords that capture the domain/business and entities as well as the session’s essence(s)—brainstorming, planning, implementation, research, or anything else of this nature, or any combination thereof. Think: “If someone wanted to find past sessions for domain context, or to recall a useful technique or piece of information on a specific topic, what keywords would help them discover this session?” Keywords should be MECE.
#   prominent_tech_stack (optional str[]): important tech stack used or discussed, if it all. usually between 0 and 2 items. this field should heavily optimize for precision disregarding recall (e.g. high session-relevancy threshold for including any item)
#   new_files (optional str[]): list of new files created in the session.
#   edited_files (optional str[]): list of files edited in the session.
#   branch (optional str): the branch of the session, if applicable.
#   jira_issue_id (optional str): the JIRA issue ID of the session, if applicable.
#   forked (optional str): the session ID the attached session was forked from, only if explicitly referenced in the beginning of the text.
# ```

# Ignored Sessions List. Schema:
# ```yaml
# ignored: <SessionID[]>
# ```

# -------
# Example sessions.yaml file:
sessions:
  # Apr 11 26
  rollout-2026-04-11T16-31-11-019d7cbd-4424-7360-ac86-fcb3c982cbd3:
    description:
      - "Unified `ccc`'s supported-session search space so negative recent selectors and `search` now traverse Claude Code, Codex, and PI sessions together instead of keeping recency resolution Claude-only."
      - "Drove the fix with a new cross-ecosystem regression test that simulates Claude, PI, and Codex session fixtures with deliberate recency ordering, then aligned the docs to describe the shared discovery model and adapter-owned sidechain filtering."
    directories: [~/dev/conversations, ~/dev/gchat, ~/dev/codexchats]
    keywords: [search-space, codex-sessions, tdd]
    prominent_tech_stack: []
    updated_when_message_count_was: 7
    new_files: [tests/test_session_search_space.py]
    edited_files: [src/conversations/parsing.py, src/conversations/commands.py, src/conversations/cli.py, src/conversations/__init__.py, README.md, ARCHITECTURE.md, CHANGELOG.md]
    branch: main
  # Apr 10 26
  019d76a2-cbed-72d0-9551-ea5a8ad568de:
    description:
      - "Added Codex-session support to `ccc` after researching real Codex JSONL history alongside `codexchats`, so Codex conversations can be parsed and resolved much like Claude and PI sessions."
      - "Covered the new behavior with TDD-style tests, clarified the default Codex preamble filtering policy, and aligned shell tests with the project's Python 3.13+ runtime; full suite green."
    directories: [~/dev/conversations, ~/dev/codexchats, ~/dev/gchat]
    keywords: [codex-sessions, jsonl-adapter, py313-tests]
    prominent_tech_stack: []
    updated_when_message_count_was: 53
    new_files: [tests/test_codex_sessions.py]
    edited_files: [src/conversations/parsing.py, src/conversations/commands.py, tests/lib.sh, tests/test_basic.sh, tests/test_colors.sh, tests/test_format.sh]
    branch: main
  # Apr 05 26
  rollout-2026-04-05T13-56-06-019d5d49-210f-7c70-a302-e70162ba68bb:
    description:
      - "Added role-visibility CLI flags (`--only-user`, `--only-assistant`, `--no-user`, `--no-assistant`) to default parse mode, composing cleanly with the existing `--thinking`, `--tools`, and `--agents` additive flags. Contradiction handling (e.g. `--only-user` with `--thinking`) is normalized upstream in `cli.py` with a warning and explicit flag disabling; the downstream pipeline receives clean booleans and needs no branching logic."
      - "TDD via vertical slices covering all contradiction combinations — `--only-assistant` overriding additive flags, `--only-user --only-assistant` naturally yielding nothing, `--no-assistant --thinking` showing only thinking blocks. Full suite green; README, ARCHITECTURE, and CHANGELOG updated."
    directories: [~/dev/conversations]
    keywords: [cli, role-visibility, tdd]
    prominent_tech_stack: []
    updated_when_message_count_was: 31
    new_files: [tests/test_parse_visibility_flags.py]
    edited_files: [src/conversations/cli.py, src/conversations/model.py, src/conversations/parsing.py, src/conversations/commands.py, README.md, ARCHITECTURE.md, CHANGELOG.md]
    branch: main
  # Apr 04 26
  rollout-2026-04-04T22-59-20-019d5a14-1c98-7601-939e-637746986944:
    description:
      - "Introduced a `JsonlSessionAdapter` registry in `parsing.py` — the first adapter being PI (`~/.pi/...` paths) — enabling JSONL session formats from agents other than Claude to plug into the shared `Message` model without branching the core pipeline. PI adapter supports user/assistant text, thinking blocks, tool calls, and tool results. TDD throughout; 183 tests passing."
      - "Extended `ccc <session-id>` with a fallback cascade: Claude Code sessions are searched first, then each adapter's own session-matching logic is tried in registry order (currently Claude → PI). Confirmed with real PI UUID `9a27c7d8-d58f-4179-bf0a-a4657c7dca64`; 185 tests passing. Follow-up discussion revealed that while the PI parser correctly iterates the ordered `message.content` list, the bucketed `Message` model fields (`text`, `thinking`, `tools`) lose the original inter-item order — a known gap."
    directories: [~/dev/conversations]
    keywords: [pi-sessions, jsonl-adapter, session-resolution]
    prominent_tech_stack: []
    updated_when_message_count_was: 50
    new_files: [tests/test_pi_sessions.py]
    edited_files: [src/conversations/parsing.py, src/conversations/commands.py]
    branch: main
  # Mar 17 26
  0f24e0cc-0cab-4011-9cfa-6f92fb016da5:
    description: "Investigated which JSONL message shapes carry a `model` field (script over 50 project dirs confirmed only `assistant` entries have it at `message.model`); moved the `model` XML attribute from being gated behind `agent_id` to all assistant messages with `.removeprefix('claude-')`; added two tests — one asserting regular and agent assistant messages include `model=`, one asserting user messages never do."
    directories: [~/dev/conversations]
    keywords: [model-attribute, xml-metadata, jsonl-analysis]
    prominent_tech_stack: []
    updated_when_message_count_was: 23
    edited_files: [src/conversations/model.py, tests/test_meta_user_messages.py]
    branch: main
  # Mar 15 26
  34bf1e8a-f47c-49a2-bdb6-19c7371e9b31:
    description:
      - "Fixed three visual bugs in Rich-colored output (`--color=always`): double blank line after message headings, empty line before closing XML tags, and leading space on non-first opening tags. Root cause was `console.print()` defaulting to `sep=\" \"` combined with `Markdown()` adding block-level newlines; fixed by adjusting separators, header text, and closing tag format in `formatting.py`."
      - "Discovered the fix didn't generalize to tool calls with `--all --color=never`: double blank lines appeared after `# Assistant` when followed by `<thinking>` tags. Root cause was upstream in `render_message_inner_xml` prepending `\\n` to THINKING and TOOL parts; removed those leading newlines so both XML and Rich paths render consistently. Extended `test_rich_whitespace.sh` to cover all four flag combos; all tests pass."
    directories: [~/dev/conversations]
    keywords: [rich-rendering, whitespace-bugs, tdd]
    prominent_tech_stack: [Rich]
    updated_when_message_count_was: 61
    new_files: [tests/test_rich_whitespace.sh, tests/data/1e446a9f-08fd-43ac-be72-8ce337d01dcd.jsonl]
    edited_files: [src/conversations/formatting.py, tests/lib.sh, tests/run_all.sh, tests/data/golden_xml_output.txt]
    branch: main
  # Mar 13 26
  368d87c9-03a1-42a4-9610-d64aaaed9505:
    description: "Enhanced `ccc rm` confirmation preview with richer metadata: files now show human-readable size (with commas) and line/message count; directories render as a recursive ASCII tree with per-file sizes and line counts. Implemented via four new helpers (`_human_size`, `_line_count`, `_file_meta`, `_render_dir_tree`) in `commands.py`; all 172 tests pass."
    directories: [~/dev/conversations]
    keywords: [ccc-rm, preview-metadata, ascii-tree]
    prominent_tech_stack: []
    updated_when_message_count_was: 18
    edited_files: [src/conversations/commands.py]
    branch: main
  # Mar 09 26
  dc43ce9c-4948-4d17-aa19-866fc4389772:
    description: "Fixed `cxc <codex-id> | ccc catalog` failing with 'No session IDs or file paths provided' — root cause was the UUID regex not matching Codex-format session IDs (e.g. `rollout-2026-...`); added a fallback that parses YAML frontmatter for any-format session_id and stores piped content as `preloaded_content`; refined so `_get_session_content()` is always attempted first, falling back to preloaded content only on failure."
    directories: [~/dev/conversations]
    keywords: [catalog, piped-input, codex-sessions]
    prominent_tech_stack: []
    updated_when_message_count_was: 26
    edited_files: [src/conversations/catalog/__init__.py]
    branch: main
  # Mar 07 26
  9fc38f50-14fd-48ae-9318-e68075228131:
    description: "Added `name` attribute to `tool-output` XML tags to match the existing `name` on `tool-input` tags, by threading `id_map` through `tool_to_parts()` and `_tool_result_to_parts()` in `tools.py` and `model.py`; updated one test assertion."
    directories: [~/dev/conversations]
    keywords: [tool-output, xml-formatting, tool-id-map]
    prominent_tech_stack: []
    updated_when_message_count_was: 14
    edited_files: [src/conversations/tools.py, src/conversations/model.py, tests/test_meta_user_messages.py]
    branch: main
  # Mar 01 26
  3bf9ac75-6064-446b-a173-b3dc613000cb:
    description:
      - "Designed a flexible, modular syntax for the `--tools` CLI option — supporting direction (input/output), error status, per-tool shortening, negation, order-independence, short/long modifier forms, and multiple simultaneous filters."
      - "Implemented TDD-style: tests first, then `tool_filter.py` with a declarative modifier map and flat guard-clause matching; updated `model.py`, `cli.py`, `__init__.py`, README, DEVELOPMENT.md, and ARCHITECTURE.md. All 161 unit + 12 shell integration tests pass."
    directories: [~/dev/conversations]
    keywords: [cli, tool-filtering, tdd]
    prominent_tech_stack: []
    updated_when_message_count_was: 34
    new_files: [src/conversations/tool_filter.py]
    edited_files: [src/conversations/model.py, src/conversations/cli.py, src/conversations/__init__.py, tests/test_tool_filter.py, tests/test_tool_filter.sh, README.md, DEVELOPMENT.md, ARCHITECTURE.md]
    branch: main
  # Jan 14 26
  cafe2981-9ec9-4cc2-aeed-b264fce19981:
    description: "Mapped Reports and Alerts event_id traceability requirements, identified existing propagation gaps, implemented a new integration test, full successful Flow run."
    keywords: [reports, alerts, event_id, traceability]
    custom_title: "reports and alerts event_id traceability requirements"
    prominent_tech_stack: [mongodb, langgraph]
    updated_when_message_count_was: 17
    edited_files: [tests/test_services/test_scheduled_report_service.py, tests/test_services/test_alerts_service.py]
    branch: tommy-log-request-id-VLLM-405
    jira_issue_id: VLLM-405
    forked: 047570d2-51ac-4a31-b02c-db55ce98193a
  # Jan 13 25
  047570d2-51ac-4a31-b02c-db55ce98193a:
    description: "Researched what other features are 'like' Signals in both projects"
    directories: [~/dev/reflection_agent, ~/dev/mega_agent_core_lib]
    keywords: [signals]
    prominent_tech_stack: []
    updated_when_message_count_was: 8
    new_files: []
    edited_files: []
    branch: null
    jira_issue_id: null
    forked: null
ignored: [dda16bd3-68fa-4cc9-86b5-fb6e6a532b41]
