#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/common.sh"
sync_skills

# Stage so symlinks are committed; omitted in post-merge since the merge already happened.
for target in "${SKILLS_TARGETS[@]}"; do
  git add "$REPO_ROOT/$target/skills"
done
