[package]
name = "two-face-boundaries"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde_json = "1"
two-face = { version = "0.5.2", default-features = false, features = ["syntect-default-fancy"] }

