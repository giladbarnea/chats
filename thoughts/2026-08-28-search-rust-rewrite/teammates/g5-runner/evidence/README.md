# Raw evidence kept because it cannot be re-taken

**`final-live-pass.log`** — the raw output of the final live consultation of the
Python search authority, 2026-09-01T19:36:07Z–19:38:44Z. **373 comparisons across
nine twins, every one passed, with both digests re-derived after the run.**

**Kept because that run can never happen again.** `final-live-consultation.md`
transcribes its counts and provenance; this is the output they were transcribed
from. **A number in a document and the run that produced it are two different
records, and only one of them stops being obtainable.**

*Everything else this seat produced in scratch was deleted as reproducible:
timing runs re-run, binary copies re-copied, the reconstruction tree regenerated
from `git archive` in four steps. This one is not.*
